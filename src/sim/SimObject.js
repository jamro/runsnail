export default class SimObject {

  constructor() {
    this.parent = null
  }

  contact(simObject) {
    
  }

  separate(simObject) {
    
  }

  update(world) {
    
  }

  destroy(world) {
    
  }
}